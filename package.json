{
	"name": "root",
	"private": true,
	"description": "A zero-config toolchain for chayns-developers.",
	"license": "MIT",
	"workspaces": [
		"packages/**"
	],
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"dev": "lerna run dev --parallel --no-private",
		"format": "prettier . --write",
		"prepare": "husky install && shx rm -rf .git/hooks && shx ln -s ../.husky .git/hooks",
		"test": "lerna run test --no-bail --stream"
	},
	"devDependencies": {
		"husky": "^6.0.0",
		"lerna": "^4.0.0",
		"prettier": "^2.2.1",
		"prettier-plugin-packagejson": "^2.2.10",
		"pretty-quick": "^3.1.0",
		"shx": "^0.3.3"
	}
}
